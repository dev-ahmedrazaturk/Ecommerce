module.exports = {
  maxWorkers: 1, // Disable parallelism temporarily to avoid the error
  testEnvironment: 'node', // Use Node environment for tests
  verbose: true, // Make Jest show individual test results
};
